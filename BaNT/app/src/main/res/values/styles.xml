<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppWarning" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorWarning</item>
        <item name="colorPrimaryDark">@color/colorWarning</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppFound" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorFound</item>
        <item name="colorPrimaryDark">@color/colorFound</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

</resources>
